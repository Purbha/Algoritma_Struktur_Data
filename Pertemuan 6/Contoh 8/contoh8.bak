#include <iostream.h>
#include <stdio.h>
#include <conio.h>

void main()
{
   int a,b,c;
	for (a=2;a<=10;a+=2)
   {
      b=2; c=0;
   	do
      {                                   
         c = c + b;
         if (a == b) {cout<<b<<" = "<<c;} else {cout<<b<<" + ";}
         b = b + 2;
      }while(b<=a);
      cout<<endl;
   }
   getch();
}